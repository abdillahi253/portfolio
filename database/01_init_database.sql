-- ==============================================
-- Portfolio Database Initialization Script (MySQL)
-- ==============================================

-- Create database (run this as root or admin user)
-- CREATE DATABASE portfolio_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- CREATE USER 'portfolio_user'@'localhost' IDENTIFIED BY 'your_secure_password';
-- GRANT ALL PRIVILEGES ON portfolio_db.* TO 'portfolio_user'@'localhost';
-- FLUSH PRIVILEGES;

-- Connect to portfolio_db before running the rest
-- USE portfolio_db;

-- MySQL automatically supports UUID generation with UUID() function
-- No extensions needed for MySQL
